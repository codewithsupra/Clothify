import React from "react";
import Directory from "../../components/directory/directory.component";// Corrected the import to start with a capital letter


const Home = () => {
  
  return <Directory  />;
};

export default Home;
